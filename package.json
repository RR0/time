{
  "name": "@rr0/time",
  "type": "module",
  "version": "0.1.4",
  "author": "RR0 <rr0@rr0.org>",
  "description": "EDTF parsing",
  "main": "src/index.js",
  "types": "types/*.d.mts",
  "scripts": {
    "test": "node --test",
    "test-ci": "node --test --experimental-test-coverage --test-reporter spec",
    "prepublish": "npm test && rm -Rf types && npx -p typescript tsc src/**/*.?js --declaration --allowJs --emitDeclarationOnly --outDir types"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "tsc": "^2.0.4"
  }
}
